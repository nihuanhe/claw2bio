# ---- Python ----
__pycache__/
*.pyc
*.pyo
*.egg-info/
.ipynb_checkpoints/
.mpl-cache-*/
.pytest_cache/

# ---- OS ----
.DS_Store
Thumbs.db

# ---- Website build artifacts ----
website/node_modules/
website/.vitepress/dist/
website/.vitepress/cache/

# ---- Local build outputs (skill zip packages, see plan-skill-packages.md) ----
.build/

# ---- Batch-2 skills: published 2026-09-21 (sanitized; big tif examples on COS) ----

# ---- Large raw data types (full datasets live on COS, not in git) ----
*.tif
*.tiff
# Exception: small downsampled mIF fixtures (a few hundred KB each) stay in git
!experiment-data/IF-免疫荧光/**/examples/**/*.tif
cos-staging/

# ---- Large per-run artefacts in example outputs (see plan-skill-example-completion.md D5) ----
# .rds objects are published via site zips / COS, not git
**/examples/**/output/*.rds
**/examples/**/output/**/*.rds
**/examples/**/output/**/*.log
# full per-cluster DE dumps: 35-95 MB each and regenerable by rerunning the example
**/examples/**/output/markers_all.csv
# same convention: big regenerable example outputs (60 MB annotation dump / 24 MB core alignment)
**/examples/**/output/gene_annotation.csv
**/all.concat.fa

# ---- OpenCLI web-read downloads (articles fetched for reference) ----
web-articles/
